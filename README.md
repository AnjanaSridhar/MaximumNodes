* A binary tree is either an empty tree or a node(called the root) containing a single integer value and linked to two further binary trees.
* This program aims to find the paths from start to edges.
* It aims to find the maximum number of distinct value that appear on a path starting at the root of the tree.

